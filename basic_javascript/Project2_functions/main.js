
function myFunction() {
    var sentence = "I am learning"; //establish the first variable
    sentence += " a lot from this boo!"; //concatenates the string
    document.getElementById("Concatenate").innerHTML = sentence; //displays the concatenated string
}
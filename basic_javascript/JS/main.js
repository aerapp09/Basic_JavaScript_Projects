function My_First_Function() { //This is defining my function with My_First_Functino() as the name
    var str= "This is the button text!"; //The first action in this function is defining the variable str
    document.getElementById("Button_Text").innerHTML = str; //This is then assigned using the ID element for the HTML to call?
}

{
    var Sent1="This is the beginning of the string", Sent2 ="and this is the end of the string";
    var Sent3= Sent1 + Sent2;
}
{
    X = 7 + 5;
    document.write(X);
}

window.alert("Attention, this is written in JavaScript")